# frontend/dockerfile (Example for Development)

FROM node:20-alpine

WORKDIR /app

# Copy package files
COPY frontend/package*.json ./

# Ensure devDependencies are installed by not setting NODE_ENV to production
# or by explicitly running `npm install` without the --production flag.
RUN npm install

# Copy the rest of the frontend source code
COPY frontend/ .

# The CMD is already specified in docker-compose.yml