# Node.js dependencies
# These will be installed inside the container via 'npm install' or a similar command.
node_modules/

# Build output folder (if building outside the container)
dist/
build/

# Git version control
.git/

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment variables & Secrets
# Ignore all .env files by default. If a specific production .env is needed
# for the build step, it can be explicitly re-included with a '!' rule.
.env
.env.*
!.env.production

# IDE and editor configuration
.idea/
.vscode/